# Adereso Homepage

Homepage de Adereso basada en la estructura de `index.html`, con el hero interactivo del proyecto `hero/` (tabs Helpdesk / Studio / Engage + mockups).

## Scripts

```bash
npm install
npm run dev
npm run build
```
